---
apiVersion: v1
kind: Service
metadata:
  name: nextwork-flask-backend
  namespace: default
  labels:
    app: nextwork-flask-backend
spec:
  selector:
    app: nextwork-flask-backend
  type: NodePort
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
